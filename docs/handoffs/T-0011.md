# T-0011 handoff

**Status:** done / released
**Owner:** Codex
**Implementation commit:** `f07ea3a906eff533ceda582b545c05bfb597cc25`
**Review-fix commit:** `cd6fce83dc939401ed5d6e431f16d269d51bfddf`

## Delivered

Biology catalogue scope realigned from 0610 Extended + 5090 to 0610 Extended + one combined
9700 International AS & A Level row. Existing 5090 catalogue row remains present as retired
historical metadata.

- Added one migration, `0016_realign_biology_vertical_slice.sql`.
- Seeded exactly one active 9700 catalogue identity:
  - board: `Cambridge International`
  - syllabus code: `9700`
  - qualification: `International AS & A Level`
  - track: `NULL`
  - display name: `Cambridge International AS & A Level Biology`
  - curriculum year: `NULL`
  - status: `active`
- Retired exact Cambridge International / 5090 / null-track catalogue row by status-only update.
  Migration does not set `updated_at`, replace row, write an invented actor event, or touch source,
  association, node, question, or rubric tables.
- Kept one combined active 9700 row; no separate AS/A Level rows.
- Review fix: 9700 insertion now uses `ON CONFLICT (...) DO NOTHING`. Fresh bootstrap still inserts
  approved metadata; direct historical SQL re-execution cannot overwrite later human edits.
- Updated active product/protocol, catalogue, curriculum-map, provenance workflow, question model,
  local setup, and decision documentation. D-0004 is superseded by D-0013.
- Updated active-dependent tests to use 9700 for resolver contract assertions and isolated synthetic
  syllabuses for cross-syllabus content fixtures. No disposable test fixture creates 9700 content.
- No shared contract change: syllabus code is already opaque and registry-backed.

## Files changed

- Migration: `services/core/migrations/0016_realign_biology_vertical_slice.sql`
- Core tests:
  - `services/core/internal/catalogue/postgres_store_test.go`
  - `services/core/internal/contentsource/handlers_test.go`
  - `services/core/internal/contentsource/postgres_store_test.go`
  - `services/core/internal/curriculummap/postgres_store_test.go`
  - `services/core/internal/question/postgres_store_test.go`
- Current-scope docs: `CLAUDE.md`, `README.md`, `docs/decisions.md`,
  `docs/curriculum-catalogue.md`, `docs/curriculum-map.md`, `docs/local-setup.md`,
  `docs/editorial-source-workflow.md`, `docs/provenance-catalogue-linking.md`,
  `docs/question-rubric-model.md`, `docs/content-provenance-register.md`
- Task records: `docs/tasks/active.md`, `docs/handoffs/T-0011.md`

No tracked `product/` or roadmap document exists at baseline `5e62d66`; `README.md`, `CLAUDE.md`,
curriculum docs, decision log, and local setup are current scope authorities. Historical task
history, handoffs, inventory, and 0610/5090 syllabus-map artifacts remain unchanged as historical
records.

## Migration behavior

- Fresh database: migrations 0001–0016 apply; active Biology codes are 0610 and 9700; 5090 is
  present and retired.
- Tracked rerun: migration runner reports `migrations applied: 0`.
- Direct SQL rerun: integration test applies permitted human changes to 9700 `display_name`,
  `curriculum_year`, and `status`, captures full catalogue/dependency state, then executes migration
  0016 twice. 9700 id, timestamps, every catalogue field, event count, and source/node/question/
  rubric counts remain identical. 5090 identity/timestamps/history/source count also remain stable.
- 9700 conflict behavior is a no-op. Unique `(board, code, COALESCE(track,''))` index prevents a
  duplicate combined Cambridge 9700 row without normalizing an existing row.
- 9700 active resolver returns exactly one id; 5090 active resolver returns not found.
- Fresh assertions and integration test confirm zero 9700 content sources, curriculum-map nodes,
  questions, and rubric versions.

## Checks

| Command | Result |
| --- | --- |
| Docker `gofmt -w` on changed Go files; Docker `gofmt -l` check | Pass; final check returned no files |
| Docker `go build ./...` | Pass |
| Docker `go vet ./...` | Pass |
| Docker `go test ./...` | Pass; all Core packages green |
| `docker compose -f docker-compose.test.yml up -d` + `pg_isready` | Pass; disposable `sidus-test` only |
| Fresh Docker `go run ./cmd/migrate` | Pass; 16 migrations applied including 0016 |
| Fresh SQL scope/content assertions | Pass; all 9 predicates true: exact 0610/9700 active, 5090 retired/present, no 9700 content, historical 5090 source retained |
| Second Docker `go run ./cmd/migrate` | Pass; `migrations applied: 0` |
| Focused 9700 direct-rerun regression test before fix | Expected fail; `DO UPDATE` reset human display/year/status |
| Focused 9700 direct-rerun regression test after fix | Pass; full 9700 state and related counts unchanged |
| Docker `go test ./... -run Integration -v` with `TEST_DATABASE_URL` | Pass; full catalogue/content-source/curriculum-map/question integration suite |
| `docker compose -f docker-compose.test.yml down -v` | Pass; test container/network removed |
| `npm.cmd --prefix apps/web run test` | Pass; 16 files, 123 tests |
| `npm.cmd --prefix apps/web run typecheck` | Pass |
| `npm.cmd --prefix apps/web run build` | Pass; Next.js production build |
| `python -m pytest` from `services/ai` | Pass; 18 tests (existing Python 3.14 deprecation/cache warnings only) |
| `apps/web/node_modules/.bin/tsc.cmd --noEmit --strict packages/shared/src/contracts.ts` | Pass |
| `docker compose -f docker-compose.yml config` | Pass |
| `docker compose -f docker-compose.test.yml config` | Pass |
| `git diff --check` | Pass |

Two initial harness invocations were corrected without code changes: PowerShell blocked `npm.ps1`
(rerun with `npm.cmd`), and Python run from repository root could not import `app` (rerun from
`services/ai`, matching project instructions). A diagnostic SQL `CASE` used an intentionally
failing constant branch that PostgreSQL pre-evaluated; direct boolean query returned all nine
predicates true, and integration coverage independently proves same behavior.

## Decisions / assumptions

- Status-only 5090 update intentionally preserves stored timestamps. Lifecycle status itself must
  change to implement retirement.
- Migration bootstrap data carries no invented Clerk actor; existing immutable audit history is
  preserved and receives no synthetic event.
- Existing seeded 5090 source metadata remains historical and untouched. It cannot resolve for a
  new association while catalogue row is retired.
- Future 9700 source must enter through editorial source registry with human-verified rights and
  provenance. No source metadata was inferred from local material.

## Protected-file status

- No local Coursebook PDF opened, parsed, hashed, extracted, ingested, copied, summarized, or
  committed.
- No PDF, textbook, syllabus text, objective, question, rubric, diagram, past paper, mark scheme,
  OCR output, or derivative content added.
- Existing migrations 0001–0015 unchanged.
- No content-source approval, link, event, node, question, or rubric seed changed.
- Untracked user artifacts (`.claude-flow/`, `.claude/`, JPEGs, and spreadsheets including roadmap
  workbooks) untouched and excluded from commit.
- Disposable `sidus-test` database only; no dev, staging, or production database mutation.

## Open questions / blockers

None.

## Release

Released from `main` after independent final validation on 2026-08-05. Do not create 9700
source/content or reactivate 5090 without a new approved task/decision.
