# T-0026 — Local Exam Mode MVP

## Status

Review. Review fixes added locally; not pushed or released.

## Delivered

- New authenticated `/dashboard/exam` surface. Recognized roles only.
- Active-syllabus picker, optional opaque curriculum-node filter, and deterministic first-in-list
  selection of 2–10 eligible MCQs.
- One-at-a-time answer selection, Back/Next, flags, answered/flagged counts, and a 30-minute
  browser-local timer. Timer uses latest selected answers at expiry and continues while submit
  confirmation is open. Refreshing loses in-memory exam; timer is not server-authoritative.
- Explicit accessible submit confirmation: initial focus, Tab containment, and return focus to
  submit trigger on cancellation. Answered questions finalize sequentially through existing learner
  create-attempt/submit-attempt routes. Unanswered questions create no attempt.
- Runtime retry keeps created attempt IDs and completed results, so current-page retries do not
  repeat a create or completed submit.
- Result review exposes only existing learner-safe marking response after finalization. Its
  aggregate is labelled **Answered score** because unanswered questions create no attempt and do
  not expose a trustworthy maximum mark.

## Scope proof

- No Core, AI, database, migration, shared contract, BFF route, or Practice Mode file changed.
- No source, locator, provenance, licence, PDF, private path, fixture, seed, educational text, or
  credentials added.
- Existing learner BFF remains fixed-operation and is reused unchanged.

## Tests

- Focused Vitest: finalization sequential/retry behavior; Exam UI insufficient-question gate,
  no early feedback/full finalization behavior, fake-timer expiry with latest answers, continued
  timer during confirmation, result denominator copy, and accessible dialog focus behavior.
- Playwright journey now creates two opaque synthetic MCQs and proves full Exam Mode start,
  answer, confirmation, submit, and two-answer result path. No source-derived text is used.

## Validation run

- Focused Exam Vitest after review fixes: 7 passed.
- Full web Vitest after review fixes: 333 passed.
- Web TypeScript and production build after review fixes: passed; `/dashboard/exam` emitted.
- `git diff --check`: passed.
- Authenticated Playwright rerun remains pending: current `localhost:3001` does not serve the
  expected app route (`404`), so no browser test was started and no synthetic runtime record was
  created by this follow-up. Start documented local web process with T-0025 environment, then run
  the `journey` project. Core TLS preflight had previously passed with private CA.

## Required validation before release

- `npm --prefix apps/web run test`
- `npm --prefix apps/web run typecheck`
- `npm --prefix apps/web run build`
- authenticated local Playwright journey, with only opaque runtime records
- `git diff --check`, staged allowlist, protected/private/source-content scan

## Remaining gaps

- No server-owned exam session, durable resume, authoritative timer, adaptive selection, free
  response, or admin paper builder.
- Real exam requires human-approved sources, verified nodes, verified questions/rubrics, and at
  least requested number of eligible MCQs. No content is created by this task.
- Independent review required before status becomes done/released.

## E2E repair update

- Reproduced the previously reported second-question failure against the live local stack. The
  helper used a corrupted node-label separator, so Playwright could not select the verified node.
- Corrected the selector to the UI's em-dash label. Subsequent full-journey execution exposed two
  test-only selector ambiguities now fixed: Practice prompt/card matching is exact, and the Exam
  navigation selector uses the exact `Next` button rather than matching the Next.js dev-tools
  control.
- The journey now captures the selected verified-node UUID from the editorial form and supplies it
  to Exam Mode. This proves the requested two-question paper contains the two questions created by
  this run, rather than an unrelated previously-created synthetic question.
- Focused authenticated journey: passed. Full authenticated Playwright suite: 10 passed. Web
  typecheck and production build: passed. `git diff --check`: passed.
- No source/private content was accessed. The E2E harness created only opaque runtime synthetic
  records in the isolated local database. Task remains `review`; no push or release.
