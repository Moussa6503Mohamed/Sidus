# T-0030 handoff — private upload and review intake

**Status:** done / released.

**Review fixes:** private upload volume mounts only into `core`, never migration service; mount
persistence proved with temporary non-PDF marker then removed. BFF rejects missing/invalid or
oversized `Content-Length` before multipart parsing, then streams into bounded memory before
FormData. Manual scan-clean route/button removed; absent scanner leaves quarantine locked, and
future scanner contract attests exact object ref plus SHA-256. Non-admin screen returns before
any fetch; covered by UI test.

Implemented admin-only private PDF quarantine foundation.

- Migration `0022_create_private_upload_intake.sql`: metadata, immutable events, review jobs.
- Core routes: list/upload, queue review, request deletion. They mount only when `SIDUS_PRIVATE_UPLOAD_DIR` is an absolute private path. Scan-clean is deliberately not an admin route.
- Local storage uses opaque references and mode `0600`; no serving endpoint exists.
- Editorial BFF fixed routes; admin UI: `/dashboard/editorial/uploads`.
- AI contract metadata only; zero model/API/OCR/extraction calls.

Review: verify no file bytes in database/API/learner routes/logs; invalid type/signature/name/size rejection; source gate; audit immutability; private path remains outside Git.

No PDF, source text, model output, API key, or runtime upload added.
