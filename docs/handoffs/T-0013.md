# T-0013 handoff

**Status:** done / released
**Owner:** Codex
**Implementation commit:** `7f4126fe44a6575253d8dc24f954e72930f43e8d`

## Delivered

- Additive, rerunnable migration 0017 adds nullable JSONB `questions.options`; no existing migration
  changed and no content seeded. Existing rows remain valid with NULL.
- Shared contracts add `MultipleChoiceOption`, nullable question response options, discriminated
  create request, and `RubricAnswerKey` inside rubric structure.
- Core accepts exact ordered 2–6 `{id,label}` options only for MCQ. Stable unique IDs are bounded to
  64 Unicode code points; original labels to 1,000. Non-MCQ rejects supplied options. MCQ-to-non-MCQ
  PATCH clears options atomically.
- Exact token decoding now rejects duplicate request keys as well as unknown/case-variant keys and
  trailing JSON before store calls. Nested options and answer key apply same exact rules.
- Option PATCH increments content revision once, records only `options` and `contentRevision`, and
  stales older verified rubrics. Rejected/no-op writes do not mutate.
- Rubric JSON accepts exact optional `answerKey:{correctOptionId}`. MCQ rubric creation requires it
  and binds it under question row lock to current stable option ID. Non-MCQ rejects it. Existing
  immutable rubric JSON and current-revision verification rules remain unchanged.
- Existing fixed editorial BFF routes forward MCQ question/rubric bodies verbatim; no operation or
  open proxy added. Draft MCQ UI adds/removes/reorders options, edits IDs/labels, and selects correct
  answer only from current options. No defaults/sample prompts/options exist. Locked lifecycle and
  current/stale rubric presentation remain clear.
- `docs/reviews/T-0013-assessment-delivery-gap-audit.md` is tracked unchanged as read-only audit
  record. D-0015 and delivery/model/setup/current-state documentation added or updated.

## Answer-key safety

Answer key remains editorial rubric-version data and is returned only by existing editorial rubric
reads. No learner route/BFF/contract exists. Future learner delivery must use separate projection
that omits rubric documents and `answerKey`; reuse of editorial rubric contract is explicitly
forbidden in delivery-schema documentation.

## Test coverage

- Options: exact schema, cardinality, blank/duplicate/bounded IDs, blank/bounded labels, types,
  unknown/case-variant/duplicate keys, trailing JSON, response-type rules, create/read/order.
- Rubrics: MCQ key required, option ID membership, non-MCQ rejection, answer-key strict schema,
  unchanged criteria/max-mark matrix, pre-mutation rejection.
- Revision/audit: successful options change +1 exactly once, names-only event, no-op/rejected no
  mutation, verified rubric becomes stale.
- Migration/Postgres: source guard, fresh bootstrap, migration runner rerun, direct 0017 double
  rerun, existing NULL row, real MCQ write/read/rubric/update/staleness path.
- Editorial/BFF: verbatim options/answer-key forwarding, no option UI outside MCQ, empty controls,
  authored payload, current-option selector, locked non-draft editing, existing role/fail-closed
  suites.

## Validation

| Check | Result |
| --- | --- |
| Docker daemon | Pass — Docker Desktop client/server 29.4.2 |
| Dev/test Compose config | Pass / Pass |
| Web Vitest | Pass — 21 files, 181 tests |
| Focused MCQ BFF/UI validation | Pass — 3 files, 22 tests |
| Web typecheck | Pass |
| Web production build | Pass — no learner route added |
| Strict shared TypeScript | Pass |
| Dockerized Go changed-file gofmt/build/vet/unit | Pass — all packages |
| Fresh disposable migration / runner rerun | Pass — 17 applied / 0 applied |
| Direct 0017 rerun | Pass — two direct reruns |
| Options revision / current-option answer-key binding | Pass — PostgreSQL integration |
| Full Core integration tests | Pass — catalogue, contentsource, curriculummap, question |
| Disposable `sidus-test` teardown | Pass — container/network removed |
| Python pytest | Pass — 18 tests; dependency deprecation warnings only |
| `git diff --check` | Pass before release commit |

## Scope and blockers

No learner endpoint/BFF/UI, sessions, attempts, AI/Anthropic, marking, explanations, timers, Exam
Mode, Redis, OCR/ingestion, source content, or seeded/hard-coded application content added. No
blockers. Independent release validation passed on 2026-08-06; explicit release approval given.

## Protected-file status

Never edited, staged, moved, deleted, or read for task content: `.claude/`, `.claude-flow/`,
`DB.jpeg`, `arch.jpeg`, `Sidus*.xlsx`, `.env.local`, PDFs, ZIPs, and design bundles. Existing
protected untracked files remain untouched. Next build reported local `.env.local` environment but
no value was printed and agent did not open or change file.

## Release content audit and remaining gates

- Release diff is documentation only: `CLAUDE.md`, task history/state, and this handoff.
- No question/rubric/source material, PDFs, secrets, design artifacts, runtime data, or protected
  user files are staged.
- `docs/reviews/T-0013-assessment-delivery-gap-audit.md` remains documentation only and unchanged by
  release work.
- Runtime Clerk/Core environment configuration remains operator-owned. Human editorial users must
  author/review content. Future learner delivery must use an answer-key-free projection.
