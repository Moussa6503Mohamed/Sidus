# T-0017 handoff — Sidus Observatory visual system and responsive frontend polish

**Status:** review (not released, not pushed)
**Owner:** Claude Code agent, 2026-08-06
**Base commit:** `f00d86176b436ae1811bc153c5a35a12e5312f82` (clean `main`)

## Summary

Applied the "Sidus Observatory" visual design system to `apps/web` presentation only, from a
user-supplied Claude artifact (`f3b99de0-8ba5-42d9-a9b4-080d86b9cc31`), fetched and read in full.
See `docs/sidus-observatory-design-system.md` and D-0019 for the complete specification and
rationale. No Core, AI, database, migration, dependency, route, or business-rule change.

## What changed

**New foundation:**
- `apps/web/styles/tokens.css` — full light/dark token set, global reset, skip-link/
  visually-hidden utilities, reduced-motion rule.
- `apps/web/components/theme/` — `ThemeToggle.tsx` (+ test), `theme-script.ts` (FOUC-free
  bootstrap, constant string), `ThemeToggle.module.css`.
- `apps/web/components/brand/Logo.tsx` (+ `Logo.module.css`) — original inline-SVG `A*` mark.
- `apps/web/components/nav/` — `RoleChip.tsx`, `nav.module.css`.
- `apps/web/components/ui/` — `Button`, `Message`, `Skeleton`, `StatusBadge` (+ test),
  `icons.tsx` (shared glyphs), each with a CSS module.
- `apps/web/lib/design/status.ts` (+ used by `StatusBadge`) and
  `apps/web/lib/design/option-state.ts` (+ test) — the two central helpers required by scope.

**Applied to existing surfaces (visual only, `git diff` confirms no logic/route/API change beyond
what's itemized below):**
- `app/layout.tsx` — tokens import, skip link, Logo, top nav restyle, RoleChip, ThemeToggle.
- `app/page.tsx` (+ new `page.module.css`) — hero typography, `Button` for sign-in/up.
- `app/dashboard/page.tsx` (+ new `dashboard.module.css`).
- `app/dashboard/practice/page.tsx`, `.../sources/page.tsx`, `.../curriculum/page.tsx`,
  `.../questions/page.tsx` — `id="main"` moved onto each page's own `<main>` (the skip-link
  target; layout no longer double-wraps in a second `<main>`), token-based padding.
- `app/dashboard/practice/styles.module.css` — full token rewrite; new `[data-option-state]`
  selectors for the six MCQ states.
- `app/dashboard/practice/question-list.tsx` — uses `lib/design/option-state.ts`; options are
  `role="radiogroup"`/`"radio"` before marking, `role="list"`/`"listitem"` after; added a
  `sidus-visually-hidden` outcome sentence ahead of the visible feedback.
- `app/dashboard/editorial/sources/styles.module.css`, `.../curriculum/styles.module.css`,
  `.../questions/styles.module.css` — full token rewrite; removed now-superseded
  `.badge[data-status=...]` rules (StatusBadge owns that now).
- `app/dashboard/editorial/sources/status-badge.tsx`, `.../curriculum/status-badge.tsx` — now
  thin wrappers around `components/ui/StatusBadge`.
- `app/dashboard/editorial/questions/question-list.tsx` — inline status `<span>` replaced with
  `StatusBadge`.
- `app/dashboard/editorial/questions/rubric-workspace.tsx` — rubric-version lifecycle status uses
  `StatusBadge`; the separate "freshness" (current/stale vs. question content revision) and
  "canonical" marker concepts got their own small token-based classes (`.freshness`,
  `.canonicalBadge`) since they are not lifecycle statuses.

**Test-infra fix (unrelated to the design system itself, needed to make the new ThemeToggle test
reliable):** `apps/web/vitest.setup.ts` now installs an in-memory `localStorage` shim when the
host Node's experimental global `localStorage` is present but missing methods like `.clear()`
(observed on Node 25 in this environment — jsdom cannot redefine an already-present global). No
change to `vitest.config.ts` was needed in the end.

## Test changes and why

Three existing test files needed updates because the visible text/ARIA role they render changed
(explicitly permitted scope — "update component tests only where changed semantics require it"):

1. `app/dashboard/practice/workspace.test.tsx` — options are now `role="radio"`
   (`aria-checked`) instead of `role="button"` (`aria-pressed`) before marking; the two generic
   post-mark tags "Selected"/"Correct" became the spec's explicit per-state tags "Correct answer"
   and "Your answer · incorrect".
2. `app/dashboard/editorial/sources/workspace.test.tsx` — content-source status label is now
   "Pending review" (from `lib/design/status.ts`, matching the artifact's badge text) instead of
   the page-local "Pending".
3. `app/dashboard/editorial/questions/workspace.test.tsx` — rubric freshness text changed from
   bare "stale"/"current" to "Stale — content changed since review"/"Current revision"; the
   canonical marker changed from lowercase "canonical" to "Canonical" (and the assertion was
   scoped to the badge `<span>` since the table also has a "Canonical" column header).

New focused tests added: `lib/design/option-state.test.ts` (pure function, all six states),
`components/ui/StatusBadge.test.tsx` (every status renders an icon + visible label; draft is
dashed-bordered; retired is struck-through — i.e. never colour-only), and
`components/theme/ThemeToggle.test.tsx` (default/persisted-read/full cycle + `data-theme` +
`localStorage` side effects).

## Known gap (documented, not silently dropped)

Practice Mode's MCQ options are individually `Tab`-focusable native `<button>`s rather than a full
roving-tabindex radiogroup with arrow-key navigation between them, which the source artifact's
accessibility section describes as the target pattern. Click/Tab/Space/Enter all work correctly.
Implementing roving tabindex was deferred to avoid a larger change to existing keyboard-interaction
wiring within this pass — see `docs/sidus-observatory-design-system.md` "MCQ option states".

## Validation

| Check | Command | Result |
| --- | --- | --- |
| Web unit/component tests | `npm --prefix apps/web run test` | **276/276 passed** (31 files) |
| Web typecheck | `npm --prefix apps/web run typecheck` | clean, no errors |
| Web production build | `npm --prefix apps/web run build` | succeeded (Turbopack, all 15 static pages + all dynamic routes) |
| Core Go build/vet/test | `docker run --rm -v "$(pwd)/services/core:/app" -w /app golang:1.22-alpine sh -c "go build ./... && go vet ./... && go test ./..."` | all packages **ok**, no vet findings |
| Python tests | `.venv/Scripts/python.exe -m pytest` (services/ai) | **18/18 passed** |
| `packages/shared` strict typecheck | covered transitively by `apps/web` `tsc --noEmit` (no `packages/shared` file was touched) | clean |
| Compose config | `docker compose -f docker-compose.yml config`, `-f docker-compose.test.yml config` | both OK |
| `git diff --check` | — | exit 0; only pre-existing LF→CRLF line-ending advisories, no real whitespace errors |
| Staged-content / secret audit | manual `git diff` review + pattern grep for key/secret/token strings | clean — no secrets, no PDFs/images/spreadsheets/`.env.local` touched |

## Protected-file status

No file under `services/core`, `services/ai`, `packages/shared`, or any migration was modified.
`.claude/`, `.claude-flow/`, images, spreadsheets, and `.env.local` (all pre-existing untracked
files in this working tree, not created by this task) were left untouched and unstaged.

## Files changed

All changes are under `apps/web/` plus `docs/` (this handoff, the design-system doc,
`docs/decisions.md` D-0019, `docs/tasks/active.md`, and this file). Run `git status --porcelain`
against base commit `f00d861` for the exact list; no file outside `apps/web/` and `docs/` changed.

## Commit

One implementation commit, made after this handoff is written. Not pushed.

## Blockers

None. Task remains status **review** per instruction — not moved to `docs/tasks/history.md`, not
released, not pushed.
