# T-0032 handoff — persistent assessment sessions

## Status

Review.

## Delivered

- Additive migration `0023_create_assessment_sessions.sql`.
- Session, immutable item snapshot, versioned response, event records.
- Learner Core routes: create, get, autosave, submit, result.
- Closed learner BFF operation union and fixed routes.
- Exam setup now creates a server-owned session; response saves use optimistic versions; timer uses server deadline; submit is retry-safe.

## Security

- Owner-scoped routes, strict JSON, bounded bodies, ID validation before Core fetch.
- Snapshot is learner-safe `Projection` only. No answer keys, rubrics, provenance, source facts, or feedback before finalization.
- Existing pinned attempt marker is invoked inside session submit transaction, so attempt writes and session closure commit together.

## Validation

- Core build/vet/unit passed.
- Fresh disposable database applied 23 migrations and existing full integration suite passed.
- Focused Exam UI test passed after server-session compatibility adaptation.
- Web typecheck and production build passed.

## Known review focus

- Add dedicated authenticated E2E coverage against local Clerk stack before release.
- Add a dedicated session integration fixture covering ownership, deadline, conflict, duplicate submit, and snapshot immutability.
