# Handoff — T-0016 Practice Mode MCQ attempts

## Status

`review`. Implementation complete. Independent review required before `done`. Not pushed or
released.

## Outcome

- MCQ rubric versions now require exact complete verified feedback tied to current option IDs.
  Non-MCQ feedback is rejected. Historical rows are untouched; no backfill/seed/content added.
- Migration 0019 adds owned pinned `open | submitted` attempts and append-only names-only events.
- Core creation atomically rechecks T-0015 eligibility and pins exact revision/canonical rubric;
  submit row-locks own open attempt, marks once from pinned rubric, and returns exact safe result.
- Every recognized role uses identical owner-only learner routes. No attempt administration.
- Separate closed learner BFF adds fixed create/submit routes with pre-auth input validation,
  fail-closed config/token behavior, redirect rejection, and 5xx sanitization.
- Practice UI adds explicit guarded submit, retry, selected/correct text labels, score, correct
  explanation, and every wrong-option explanation. Editorial rubric form authors required data.
- No Exam Mode, AI, timer, paper/session flow, fallback rubric, source-derived feedback, analytics,
  or generated educational prose.

## Changed areas

- Core: `services/core/internal/question`, `services/core/internal/learner`, auth role matrix.
- Database: `services/core/migrations/0019_create_learner_mcq_attempts.sql`.
- Web: learner proxy/routes; Practice client/UI/tests; existing editorial rubric authoring form.
- Contracts/docs: `packages/shared/src/contracts.ts`, D-0018,
  `docs/practice-mcq-marking.md`, setup/protocol/task files.

## Validation

| Check | Result |
| --- | --- |
| Dev/test Compose `config` | Pass / pass |
| Web Vitest | Pass — 28 files, 252 tests |
| Web TypeScript | Pass |
| Web production build | Pass — learner create/submit routes present |
| Shared strict TypeScript using installed compiler | Pass |
| Go `gofmt`, build, vet, unit suite | Pass — all packages |
| Fresh disposable `sidus-test` migrations | Pass — 19 applied |
| Same database migration rerun | Pass — 0 applied |
| Full disposable integration suite | Pass, including ownership/pinning/parallel submit |
| Disposable teardown | Pass — `sidus-test` container/network removed |
| Python tests | Pass — 18 tests; environment emitted existing deprecation/cache warnings |
| `git diff --check` | Pass |
| Staged content/secret/protected audit | Pass; see commit preparation below |

`npx -p typescript` initially attempted registry access and failed under restricted npm cache/network.
No dependency was needed; same strict command passed using checked-out web `node_modules` compiler.

## Security/reviewer focus

- Review SQL row locks and immutable pins/events in migration/store.
- Review strict duplicate-key/case/type/coverage decoder matrix and feedback verification path.
- Review owner-filtered lookup, stable replay conflict, and exact learner response field tests.
- Review learner BFF exact-body parser and separation from editorial proxy.
- Review UI retry retaining created attempt ID and selected/correct non-color labels.

## Commit / protected files

One implementation commit includes this handoff. Exact hash is reported from `git rev-parse HEAD`
after commit because a commit cannot contain its own hash. Protected pre-existing untracked files
(`.claude/`, `.claude-flow/`, images, spreadsheets) and ignored `.env.local` were not touched or
staged. No PDF, ZIP, design bundle, source material, or runtime data is included.

## Blockers

None. Stop at independent review; do not release or push.
