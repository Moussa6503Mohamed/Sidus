# T-0012 handoff

**Status:** done / released
**Owner:** Codex
**Implementation commit:** `e194c2527dc87482ff9d845bbb03ad56991dbc1a`

## Delivered

- Protected `/dashboard/editorial/questions` browser workflow for editor/reviewer/admin; denied
  learner/unknown state mounts no workspace and makes zero workflow API calls.
- Syllabus and verified curriculum-node selectors, all-status question list, empty create/edit
  draft form, original-content warning, response types, language, prompt, node, lifecycle,
  content-revision display, and mobile-safe existing styles.
- Rubric version list with draft/verified status, question revision, max marks, current/stale state,
  empty structured criteria editor, safe client validation, draft version create, reviewer/admin
  version verify, and question verify/retire confirmations.
- Actionable mappings for grounding/source regression, lifecycle, missing/stale verified rubric,
  invalid rubric/max marks, duplicate allocation, and no-change errors.
- Closed BFF union extended by nine fixed Core operations; six browser route shapes expose only
  intended methods. Existing syllabus and verified-node BFF operations reused.
- Minimal Core read-contract correction: existing editorial-only `question:read` list/get may see
  draft/verified/retired; list supports validated `draft`/`verified`/`retired`/`all`. No schema,
  migration, permission, write-side, grounding, lifecycle, revision, or rubric rule changed.

## Actual Core routes and BFF allowlist

| Browser route | Methods | Core route |
| --- | --- | --- |
| `/api/editorial/questions` | GET, POST | `GET/POST /questions` |
| `/api/editorial/questions/{id}` | GET, PATCH | `GET/PATCH /questions/{id}` |
| `/api/editorial/questions/{id}/verify` | POST | `POST /questions/{id}/verify` |
| `/api/editorial/questions/{id}/retire` | POST | `POST /questions/{id}/retire` |
| `/api/editorial/questions/{id}/rubric-versions` | GET, POST | `GET/POST /questions/{id}/rubric-versions` |
| `/api/editorial/questions/{id}/rubric-versions/{version}/verify` | POST | `POST /questions/{id}/rubric-versions/{version}/verify` |

List query mirrors Core exactly: required `syllabusId`, optional `curriculumMapNodeId`, optional
`status`. Existing `/api/editorial/syllabuses` and
`/api/editorial/curriculum-map/nodes?...&status=verified` supply selectors.

## Security behavior

- Fixed operation discriminants and paths only; no catch-all/open proxy or caller Core URL.
- Server-only `SIDUS_CORE_API_URL` and server-acquired Clerk bearer forwarding.
- Missing URL 503 before auth; invalid id/query/version 400 before auth/fetch; missing token 401
  before fetch; JSON content type/100 KB/syntax checks before mutation fetch.
- Core 5xx body discarded and replaced by generic 502; redirects use `redirect: "error"`; network
  and non-JSON upstream failures generic. No body, prompt, rubric, token, Core URL, or exception
  logging added.
- Web role gate is visibility only. Core permissions and all business rules remain authoritative.

## Test coverage

- BFF: fixed operation→method/path mapping for every T-0009/T-0010/T-0012 operation; unsafe ids,
  missing/unsafe syllabus, unsafe node, status, and rubric version rejected before auth/fetch;
  missing config/token, JSON envelope, Core 5xx sanitization, redirect refusal, safe 4xx passthrough,
  non-JSON/network failures, and no logging remain green.
- Route handlers: exact method exports, list/get/create/update/lifecycle/rubric delegation, fixed
  empty lifecycle bodies, body rejection, and safe errors.
- UI: role/zero-call gate, loading, empty, error/retry, verified-node loading, create, minimal draft
  edit, provenance warning, structured rubric validation, stale/current presentation, reviewer
  visibility, and verify/retire confirmation.
- Core: all-status get/list, omitted/explicit-all and lifecycle filters, invalid status, database
  filtering, existing syllabus/node validation, grounding, revision, rubric, lifecycle, and auth
  coverage.

## Validation

| Check | Result |
| --- | --- |
| Docker daemon | Pass — client/server 29.4.2 |
| Web Vitest | Pass — 21 files, 176 tests |
| Focused question BFF policy | Pass — 2 files, 72 tests |
| Web typecheck | Pass |
| Web production build | Pass — 22 dynamic routes including six question API shapes and page |
| Strict shared-contract TypeScript | Pass |
| Dockerized Go formatting/build/vet/unit | Pass — all packages |
| Dev/test Compose config | Pass / Pass |
| Fresh disposable migration | Pass — 16 applied |
| Migration idempotent rerun | Pass — 0 applied |
| Full Core integration tests | Pass — catalogue, content source, curriculum map, question |
| Disposable `sidus-test` teardown | Pass — container/network removed |
| Python pytest | Pass — 18 tests (warnings only) |
| `git diff --check` | Pass — staged diff has no whitespace errors |

## Content-safety proof

- No sample prompt, rubric descriptor/answer, source wording, syllabus wording, paper item, mark
  scheme, PDF/extract, diagram, fixture, UI default, seed, or migration added.
- Forms start empty. New tests use opaque runtime-generated prompt-shaped strings and criterion ids;
  no question/rubric prose is committed.
- No application startup/build/test path writes runtime content. Only explicitly requested isolated
  disposable integration tests created temporary test rows; teardown removed entire `sidus-test`
  stack. No dev/prod runtime data touched.

## Decisions, blockers, review gate

D-0014 documents BFF and minimal Core read correction. No blockers. Independent release
validation passed on 2026-08-05; task released from `main`.

## Protected-file status

Never edited, staged, moved, deleted, or read for task content: `.claude/`, `.claude-flow/`,
`DB.jpeg`, `arch.jpeg`, `Sidus*.xlsx`, `.env.local`, PDFs, ZIPs, and design bundles. Existing
protected untracked files remain untouched. Production build detected local `.env.local` through
Next.js environment loading but no value was printed and file was not opened or changed by agent.
