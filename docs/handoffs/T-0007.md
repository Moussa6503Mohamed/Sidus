# T-0007 — Original question and rubric foundation

**Status:** review
**Owner:** Claude Code agent
**Depends on:** T-0001 (done), T-0004 (done), T-0005 (done), T-0006 (done)

## Goal

Build private original-question and versioned-rubric infrastructure for a future Exam Mode, where
every question traces to a **verified** curriculum-map node whose approved content source still
passes the T-0006 source gate. No question text, rubric text, syllabus text, mark schemes,
past-paper content, PDFs, extracted text, diagrams, OCR output, or derivative questions are
created or seeded. No AI generation, Anthropic call, OCR, ingestion, or question derivation.

## What changed

### Database (`services/core/migrations`)

- `0012_create_questions.sql` — `questions`: `id`, `syllabus_id` (FK → `syllabuses`),
  `curriculum_map_node_id` (**NOT NULL** FK → `curriculum_map_nodes`), `response_type` (CHECK:
  multiple_choice/short_answer/structured_response), `language`, `prompt`, `status` (CHECK:
  draft/verified/retired, default draft), timestamps; indexes on syllabus/node/status.
- `0013_create_question_rubric_versions.sql` — `question_rubric_versions`: `id`, `question_id` FK,
  `version` (`CHECK > 0`), `rubric` JSONB, `max_marks` (`CHECK > 0`), `status` (draft/verified),
  `created_by`/`reviewed_by` (verified Clerk subjects), timestamps. `UNIQUE (question_id,
  version)`. **`BEFORE UPDATE OR DELETE` trigger** rejects every DELETE and any UPDATE changing
  `question_id`, `version`, `rubric`, `max_marks`, `created_by`, or `created_at` — only
  `status`/`reviewed_by`/`updated_at` may change.
- `0014_create_question_events.sql` — `question_events`: append-only audit (`question_id`,
  `event_type` CHECK over the six event types, `actor_id`, `event_time`, `changed_fields`),
  `BEFORE UPDATE OR DELETE` trigger rejecting mutation — mirrors `content_source_events` /
  `syllabus_events` / `curriculum_map_events`.

### Core Go service (`services/core/internal/question`, new package)

- `model.go` — `Question`, `RubricVersion`, `Event`, `ResponseType`, `Status`, `RubricStatus`;
  `CreateInput`/`UpdateInput`/`CreateRubricVersionInput`.
- `rubric.go` — `ValidateRubric`: the validation-safe rubric schema (non-empty bounded `criteria`
  array; unique non-blank criterion `id`; positive integer `marks`; optional non-blank
  `descriptor`; no unknown fields on document or criterion; criterion marks must sum exactly to
  `maxMarks`). Returns `ErrInvalidRubric`/`ErrInvalidMaxMarks` — never raw JSON error text.
- `store.go` — `Store` interface + domain error sentinels (`ErrNotFound`,
  `ErrRubricVersionNotFound`, `ErrUnknownSyllabus`, `ErrUnknownNode`, `ErrUnverifiedNode`,
  `ErrMismatchedNode`, `ErrUnknownSource`, `ErrUnapprovedSource`, `ErrUnlinkedSource`,
  `ErrMismatchedSource`, `ErrNoChanges`, `ErrInvalidTransition`, `ErrMissingVerifiedRubric`,
  `ErrInvalidRubric`, `ErrInvalidMaxMarks`, `ErrDuplicateRubricVersion`).
- `postgres_store.go` — `PostgresStore`: `CreateQuestion`, `UpdateQuestion`, `VerifyQuestion`,
  `RetireQuestion`, `GetQuestion`, `ListQuestions`, `CreateRubricVersion`, `ListRubricVersions`,
  `VerifyRubricVersion`. `validateGrounding` is the single gate (node exists → same syllabus →
  verified → source approved+linked+matched) and runs inside **every** write transaction before
  any row, status change, or event. Version numbers are allocated under a row lock on the parent
  question (`SELECT ... FOR UPDATE`), so they are unique and monotonic. `22P02` (malformed UUID)
  maps to the same stable domain error as a missing row.
- `handlers.go` — `Register` mounts `GET/POST /questions`, `GET/PATCH /questions/{id}`,
  `POST /questions/{id}/verify`, `POST /questions/{id}/retire`,
  `GET/POST /questions/{id}/rubric-versions`,
  `POST /questions/{id}/rubric-versions/{version}/verify`, each behind `auth.Protect`. Strict JSON
  via a shared `decodeStrict` that enforces a **case-sensitive field allowlist** per endpoint
  before the struct decode. Infrastructure failures always return the single generic
  `internal_error` message.

### Auth (`services/core/internal/auth/auth.go`)

- New permissions: `PermReadQuestion` (`question:read`), `PermCreateQuestion` (`question:create`),
  `PermVerifyQuestion` (`question:verify`), `PermReadQuestionRubric` (`question_rubric:read`).
- Role matrix: editor gets read + create + rubric-read; reviewer adds verify; admin gets all four.
  Learner and unknown remain denied (absent from the matrix).

### Wiring (`services/core/main.go`)

- `question.Register(mux, question.NewPostgresStore(db), verifier)` added alongside catalogue /
  content-source / curriculum-map registration, inside the same `DATABASE_URL` + Clerk-configured
  fail-closed branch.

### Shared contracts (`packages/shared/src/contracts.ts`)

- `QuestionResponseType`, `QuestionStatus`, `RubricVersionStatus`, `Question`, `RubricCriterion`,
  `RubricStructure`, `QuestionRubricVersion`, `CreateQuestionRequest`, `UpdateQuestionRequest`,
  `CreateQuestionRubricVersionRequest`, `QuestionEventType`, `QuestionEvent`. Strict-typechecked
  standalone (`tsc --noEmit --strict`).

### Docs

- New `docs/question-rubric-model.md`: original-content policy, the private workflow, the
  grounding chain and gate, lifecycle, rubric structure, versioning/immutability, API + strict
  decoding, roles, audit table, and the future AI boundary.
- New decision `D-0009` in `docs/decisions.md`.
- `docs/curriculum-map.md`: new "What consumes a verified node" section (retiring a node freezes
  the questions grounded in it).
- `docs/local-setup.md`: `/questions` added to the fail-closed mounting note.
- `CLAUDE.md` "Current state" updated.
- `docs/tasks/active.md` rewritten with the T-0007 entry.

## Schema decisions (also recorded in D-0009)

- **A question stores both `syllabus_id` and `curriculum_map_node_id`**, with equality enforced by
  Core on every write. Storing both keeps list-by-syllabus a single indexed read while the
  invariant stays application-enforced (a bare FK cannot express "node is verified and its source
  is still approved for this syllabus").
- **The grounding gate re-runs on every question write**, not just create — a node can be retired
  and a source can be un-approved, unlinked, or re-linked afterwards. Accepted consequence: a
  question with regressed grounding is frozen, including for retirement, until the node/source is
  restored. Same trade-off already accepted for curriculum-map nodes (D-0008 review fix 2).
- **`syllabusId` is immutable on a question**; repointing `curriculumMapNodeId` re-validates the
  syllabus match, so a cross-syllabus move is rejected rather than half-applied.
- **Rubric versions are append-only and DB-immutable.** Correcting a rubric means a new version.
  This is what makes a marked answer explainable by the exact rubric that produced it, and what
  makes the `rubric` component of the D-0003 explanation cache key well-defined.
- **Version numbers are server-allocated under a row lock**, never caller-supplied (no races, no
  gaps, no overwriting history). The unique index is the backstop → `duplicate_rubric_version`.
- **A question cannot be verified without a verified rubric version** (`409
  missing_verified_rubric`); a draft rubric does not count.
- **`status` is never PATCHable** — only verify/retire change it, so lifecycle rules live in one
  place.
- **Rubric listing has its own permission** (`question_rubric:read`) rather than reusing
  `question:read`, because it exposes draft rubric structure.

### Hardening beyond the curriculum-map precedent

`decodeStrict` now enforces a case-sensitive allowlist on **`POST` as well as `PATCH`**. Go's
struct decoding matches JSON field names case-insensitively, so `DisallowUnknownFields` alone
accepts `{"SyllabusId": ...}` as `syllabusId`; the allowlist is the only place that rejects a case
variant. (Verified by test: `TestCreateQuestion_StrictJSON/case_variant` fails with the allowlist
removed.) The `POST` paths in `curriculummap`/`contentsource`/`catalogue` still rely on
`DisallowUnknownFields` alone and therefore still accept case variants of known fields — a
pre-existing, cross-package observation, not fixed here (see "Blockers / open questions").

## Tests

### `internal/question/handlers_test.go` (in-memory store, real auth middleware)

Create success and verified-actor audit; node gate (unknown / draft / retired / other-syllabus
node); unknown syllabus; invalid response type; PATCH repoint to each invalid node and to a valid
node; grounding re-validation on PATCH, rubric create, rubric verify, question verify and retire
(6 break states each: node retired, node reverted to draft, source un-approved, unlinked,
re-linked, deleted) with no-mutation/no-event/no-rubric assertions; rubric version monotonicity,
draft status, creator identity, and listing order; rubric verify records the reviewer and is
single-use; unknown version → 404; 13 invalid rubric/marks payloads; missing rubric fields;
rubric create against a non-draft question → 409; verify without any rubric and with a draft-only
rubric → 409 `missing_verified_rubric`; full lifecycle (verify → re-verify 409 → PATCH 409 →
retire → re-retire 409); retired question hidden from get and list; draft hidden from readers;
list requires/validates `syllabusId` and honours the optional node filter; full role matrix
(learner/unknown denied on all 9 routes, missing token 401, editor drafts but cannot verify or
retire, reviewer verifies and retires); audit identity is the verified subject only; audit carries
field names only; strict JSON on POST (actorId, reviewerId, status spoofing, unknown field, case
variant, trailing object/array/junk, non-object, malformed) and on PATCH (16 cases incl.
`syllabusId`, `id`, `createdAt`, `updatedAt`, typo, case variant, trailing values) each asserting
no mutation and no event; `no_updatable_fields` / `no_changes` / `blank_fields`; and a table
asserting every domain error maps to its documented stable code, plus that an infrastructure error
maps to none.

### `internal/question/rubric_test.go`

5 accepted shapes; 19 rejected shapes across `ErrInvalidRubric`/`ErrInvalidMaxMarks`; criteria
count bound.

### `internal/question/migrations_test.go`

`TestMigrations_SeedNoQuestionOrRubricContent` — scans every `.sql` migration and fails on any
`INSERT INTO`/`COPY` targeting `questions`, `question_rubric_versions`, or `question_events`, so
question/rubric content can never be committed to this repository. `TestMigrations_QuestionTablesExist`
keeps that guard honest (it must be scanning a migration set that really creates those tables).

### `internal/question/postgres_store_test.go` (disposable `sidus-test` only)

Create + node gate (draft/retired/other-syllabus/unknown/malformed/empty node id, unknown
syllabus, single audit event); grounding re-validation on update / rubric create / rubric verify /
question verify / question retire over 5 live break states each, asserting the question row
(status, prompt, language, response type, node, `updated_at`) and the event **and** rubric counts
are byte-identical after rejection; rubric monotonicity + a direct duplicate `(question_id,
version)` insert rejected by the unique index; rubric content immutability against the DB trigger
(rewrite version / rubric / max marks / creator, and DELETE, all rejected; a no-op update
allowed); rubric validation rejected before any write; verify requires a verified rubric (none →
409, draft-only → 409, verified → success); full lifecycle with the exact five-event audit
sequence and retired-question invisibility; `question_events` immutability (UPDATE and DELETE both
rejected); audit records names-only with verified actors; list validates the syllabus and filters
by node; unknown/malformed question ids on get/list-rubrics/update/verify.

### `internal/auth/auth_test.go`

`TestQuestionPermissionMatrix` pins the four new permissions across all five roles.

## Acceptance checks

| Check | Result |
| --- | --- |
| Migrations bootstrap on an empty DB and are idempotent on rerun | Met — 14 applied on a fresh disposable DB (3 new), 0 on rerun |
| No seeded question/rubric text anywhere | Met — no seed migration added; `TestMigrations_SeedNoQuestionOrRubricContent` enforces it |
| Question syllabus must equal the mapped node's syllabus | Met — `mismatched_node`, unit + integration |
| Draft/retired/missing nodes rejected | Met — `unverified_node` / `unknown_node` |
| Verified node + source gate revalidated on every write and verification | Met — 5 write paths × 5–6 break states, unit + integration |
| Question cannot verify without a verified rubric | Met — `missing_verified_rubric` (none and draft-only) |
| Rubric versions immutable / unique / monotonic | Met — DB trigger, unique index, row-locked allocation |
| Lifecycle transitions enforced | Met — verify/retire/re-verify/re-retire/PATCH-non-draft |
| Question events immutable | Met — trigger rejects UPDATE and DELETE |
| Verified Clerk subject audit identity | Met — actor/createdBy/reviewedBy all from the session subject |
| Full role matrix | Met — `TestRoleMatrix_*`, `TestQuestionPermissionMatrix` |
| Strict unknown/trailing JSON rejection | Met — POST and PATCH, incl. case variants |
| No raw internal errors | Met — `internalErrorMessage` is the only 500 body; `TestNoRawInternalErrorText` |
| Existing test suite green | Met — see validation below |

## Validation (2026-08-04)

| Command | Result |
| --- | --- |
| `go build ./... && go vet ./...` (Docker `golang:1.22-alpine`) | Pass |
| `go test ./...` (unit, Docker toolchain) | Pass — core, auth, catalogue, contentsource, curriculummap, question all green |
| `gofmt -l .` | Pass for all new/changed files — flags only pre-existing, unrelated formatting in `internal/auth/auth_test.go` (a table at line ~182, untouched by this task) and `main_test.go` |
| `docker compose -f docker-compose.yml config` / `-f docker-compose.test.yml config` | Pass / Pass |
| `docker compose -f docker-compose.test.yml up -d` + `pg_isready` wait | Pass — `sidus-test-postgres-test-1` healthy |
| `go run ./cmd/migrate` against disposable `sidus-test` | Pass — 14 migrations applied (0012–0014 new) |
| `go test ./... -run Integration` against `sidus-test` | Pass — catalogue + content-source + curriculum-map + question (15 top-level question integration tests, 105 including subtests) |
| `go run ./cmd/migrate` rerun against the same `sidus-test` | Pass — idempotent, `migrations applied: 0` |
| `docker compose -f docker-compose.test.yml down -v` | Pass — `sidus-test` destroyed only; no `sidus` dev container existed or was touched |
| `python -m pytest -q` (services/ai) | Pass — 18 tests (AI service untouched) |
| `npm --prefix apps/web run typecheck` | Pass |
| `npm --prefix apps/web run build` | Pass — Proxy (Middleware) detected; 6 routes intact |
| `npx -p typescript tsc --noEmit --strict packages/shared/src/contracts.ts` | Pass |
| `git diff --check` | Pass — no whitespace errors |

## Protected-file status

Never staged/altered/moved/deleted: `DB.jpeg`, `arch.jpeg`, `Sidus.xlsx`,
`Sidus_Roadmap_and_Cost_Model(1).xlsx`, `Sidus_Final_MVP_Technical_Cost_Model*.xlsx`,
`Sidus_Final_MVP_Technical_Cost_Model_Recreated.xlsx`, `.claude/`, `.claude-flow/`, any
`.env.local`. Only task-relevant files were staged. Nothing was pushed.

## Blockers / open questions

None blocking.

- **Carried forward (human action, not performed here):** the two seeded 0610/5090 content sources
  are still `pending`/unlinked (T-0001 approval + T-0005 linking), and no curriculum-map node has
  been authored or verified (T-0006). Until all three happen, **no question can pass the grounding
  gate** — which is why nothing is seeded.
- **Out of scope by design:** the private, approved editorial workflow that will author question
  prompts and rubric structures at runtime. This task ships only the infrastructure it writes into.
- **Recorded assumptions:** `language` is an opaque non-empty tag (no language registry exists);
  rubric listing uses its own permission rather than reusing `question:read`.
- **Observation, not fixed (cross-package):** `POST` handlers in `curriculummap`, `contentsource`,
  and `catalogue` still accept case variants of known field names, because
  `DisallowUnknownFields` is case-insensitive on struct decoding. The `question` package closes
  this with an explicit allowlist on both verbs; retrofitting the sibling packages is worth a
  separate task (it would change their behaviour, which is outside T-0007's scope).
- **Observation, carried from T-0006:** a malformed (non-UUID) id still yields `500` in the
  sibling packages. The `question` package maps `22P02` to the stable domain error on every id it
  reads; the cross-package cleanup remains open.

## Next task

Move to `done` after independent review approval (per `docs/agent-workflow.md` — this task stays
`review` until then).
