# Infrastructure placeholders

Future local development services: PostgreSQL, Redis, S3-compatible object storage, OpenSearch, and worker queues. Add Compose manifests only once service configuration exists.
